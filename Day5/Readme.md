SELECT *              → all columns
SELECT name, salary   → specific columns

WHERE                 → filter
AND                   → both conditions
DISTINCT              → unique values
ORDER BY              → sorting
DESC                  → highest → lowest
ASC                   → lowest → highest
LIMIT                 → restrict number of rows
LIKE 'A%'             → starts with A